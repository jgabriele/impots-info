export default theme => ({
  wrapper: {
    display: 'flex',
    height: 'calc(100vh - 64px)',
    padding: '0 64px',
    alignItems: 'center',
    fontSize: '1.4em'
  },

  inputs: {
    marginRight: '2em',
    minWidth: 500
  },

  results: {
    flex: 1,
    minWidth: 400
  }
})
